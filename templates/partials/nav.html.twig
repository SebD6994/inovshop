<nav class="navbar navbar-expand-lg bg-body-tertiary fixed-top">
  <div class="container-fluid">
    <!-- Première barre (fixe en haut) -->
    <a class="navbar-brand" href="#">Euphorie Florale</a>
    <div class="d-flex">
      <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
      <a class="nav-link" href="{{ path('app_panier') }}">Panier</a>
    </div>
  </div>
</nav>

<!-- Deuxième barre (fixe après défilement) -->
<nav class="navbar navbar-expand-lg bg-light sticky-top mt-5">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCategories" aria-controls="navbarCategories" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCategories">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="{{ path('app_front') }}">Accueil</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_catalogue', {'category': 'mariage'}) }}">Mariage</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_contact', {'category': 'deuil'}) }}">Deuil</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_contact', {'category': 'bouquet'}) }}">Bouquet</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_catalogue', {'category': 'atelier-floral'}) }}">Atelier Floral</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_catalogue', {'category': 'abonnement'}) }}">Abonnement</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('app_catalogue', {'category': 'contact'}) }}">Contact</a></li>
      </ul>
    </div>
  </div>
</nav>